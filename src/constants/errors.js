export const ERRORS = {
    CREDENTIALS: 'credentials',
    CUSTOM_CREDENTIALS_NOT_ALLOWED: 'custom-credentials-not-allowed',
    NETWORK: 'network',
    OTHER: 'other',
};

export const ERROR_MESSAGES = {
    AUTHENTICATION_FAILED: 'Authentication failed',
};

Object.freeze(ERRORS);
